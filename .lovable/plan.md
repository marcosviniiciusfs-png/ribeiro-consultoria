

## Plano: Reduzir tamanho da seção de localizações

### Alteração em `src/components/BenefitsSection.tsx`

- Reduzir `max-w-5xl` para `max-w-3xl` no container principal
- Reduzir `min-h-[240px]` das imagens e iframes para `min-h-[160px]`
- Reduzir o gap entre elementos
- Manter layout responsivo mas com proporções menores

